def merge_the_tools(string, k):
    n = len(string)
    for i in range(0, n, k):
        t_i = string[i : i + k]
        u_i = ""
        for char in t_i:
            if char not in u_i:
                u_i += char
        print(u_i)
